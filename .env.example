# Get these from https://app.tina.io after you connect this repo as a project.
# Not required for local editing (`npm run dev`) — only for `npm run build`
# and for the hosted Tina Cloud editor.
TINA_CLIENT_ID=
TINA_TOKEN=
